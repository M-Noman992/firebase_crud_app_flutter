name: firebase_crud_app_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Updated for better compatibility with modern Firebase packages

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # Firebase Core and Services 
  firebase_core: ^3.10.1
  cloud_firestore: ^5.6.2
  firebase_storage: ^12.4.2

  # Utilities and UI 
  image_picker: ^1.1.2
  http: ^1.2.2
  shared_preferences: ^2.3.5
  intl: ^0.19.0 # Updated for compatibility with modern Flutter environments

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0 # Updated to a more recent stable version

flutter:
  uses-material-design: true